<div class="col-md-3">
    <p class="lead">Nos marques</p>
    <div class="list-group">
        <a href="{{ path('store_products') }}" class="list-group-item active">Toutes les marques</a>
        {% for brand in brands %}
            <a href="{{ path('store_products') }}/{{ brand.id }}" class="list-group-item">{{ brand.title }}</a>
        {% endfor %}
    </div>
</div>